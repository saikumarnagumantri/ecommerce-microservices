# The gateway's base URL. "localhost" only works when the app runs in a
# web browser or an iOS simulator on the same machine as the backend.
# Android emulator: http://10.0.2.2:3001/api
# Physical device:  http://<your-machine-LAN-IP>:3001/api
EXPO_PUBLIC_API_URL=http://localhost:3001/api
