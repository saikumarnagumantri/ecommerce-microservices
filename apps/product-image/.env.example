PORT=3005

# Must match the users service's JWT_SECRET — uploads are verified directly here.
JWT_SECRET=dev-secret-change-me

UPLOAD_DIR=uploads
PUBLIC_BASE_URL=http://localhost:3005

# Required on every request; only the gateway or another internal service should ever send this.
INTERNAL_API_KEY=dev-internal-key-change-me
